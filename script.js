// 塔罗牌数据库 - 包含正位和逆位的完整解释
const tarotCards = [
    {
        name: '愚者',
        symbol: '🃏',
        description: '一位年轻人站在悬崖边，仰望天空，怀抱纯真与梦想，即将踏上未知的旅程。',
        upright: {
            love: '新恋情即将开始，保持开放的心态。单身者会遇到意想不到的缘分，恋爱中的人需要给彼此更多自由空间。',
            career: '新的工作机会或项目即将出现，勇敢尝试新领域。现在是转换跑道或创业的好时机。',
            future: '即将迎来全新的开始，一段充满可能性的旅程正在等待你。保持好奇心和冒险精神。',
            wealth: '财务上可能有意外的机会，但需要谨慎评估风险。不要盲目投资，保持理性的同时也要敢于尝试新的理财方式。',
            health: '身体状态良好，充满活力。适合尝试新的运动或健康习惯，但要注意安全，避免过于冒险的活动。',
            relationship: '社交运势上升，会结识新朋友。保持真诚和开放的态度，新的人际关系会为你带来惊喜。'
        },
        reversed: {
            love: '感情上过于冲动或不负责任，可能做出草率的决定。需要更加成熟地对待感情，避免伤害他人。',
            career: '缺乏计划和准备，盲目冒险可能导致失败。需要更加谨慎，做好充分准备再行动。',
            future: '鲁莽行事可能带来不必要的风险。需要在自由和责任之间找到平衡，避免逃避现实。',
            wealth: '财务管理混乱，冲动消费或高风险投资可能导致损失。需要建立理财计划，避免盲目决策。',
            health: '不注意安全，可能因冒险行为受伤。需要更加谨慎，建立规律的生活习惯。',
            relationship: '社交中缺乏界限，可能被不良朋友影响。需要更加成熟地处理人际关系，学会说不。'
        }
    },
    {
        name: '魔术师',
        symbol: '🎩',
        description: '一位魔法师站在祭坛前，手指天地，桌上摆放着四大元素的象征物，代表无限的创造力。',
        upright: {
            love: '你拥有吸引力和魅力，主动出击会有好结果。善用沟通技巧，表达真实感受。',
            career: '你具备成功所需的所有技能和资源，是展现才华的最佳时机。项目会顺利推进。',
            future: '你将掌握主动权，有能力实现目标。创造力和行动力会为你带来成功。',
            wealth: '财运亨通，善用你的技能和资源可以创造财富。投资理财需要运用智慧，多方研究后再行动。',
            health: '精力充沛，身心协调。适合学习新的健康知识，将理论付诸实践会有很好的效果。',
            relationship: '沟通能力强，容易成为人群中的焦点。善用你的魅力和才华，会吸引志同道合的朋友。'
        },
        reversed: {
            love: '沟通不畅或操纵他人感情。可能言行不一，缺乏真诚。需要反思自己的动机。',
            career: '滥用才能或缺乏执行力，计划无法落实。可能遇到欺诈或被人误导。',
            future: '潜力未能发挥，资源浪费。需要更加专注和诚实，避免投机取巧。',
            wealth: '理财决策失误，可能遭遇金融欺诈。不要过于自信，需要更加谨慎和务实。',
            health: '身心失调，知识和行动脱节。需要将健康理念真正付诸实践。',
            relationship: '人际交往中不够真诚，可能操纵他人。需要建立真实的连接，而非表面功夫。'
        }
    },
    {
        name: '女祭司',
        symbol: '🌙',
        description: '神秘的女祭司坐在黑白柱子之间，手持智慧之书，背后是神秘的面纱和月亮。',
        upright: {
            love: '倾听内心的声音，直觉会告诉你答案。可能有隐藏的感情或秘密需要揭示。',
            career: '需要深入思考和研究，不要急于做决定。内在智慧会指引正确方向。',
            future: '神秘的事物即将揭晓，相信你的第六感。静心等待，答案会自然浮现。',
            wealth: '财务决策需要深思熟虑，相信直觉但也要做好调研。可能有隐藏的财务信息需要发掘。',
            health: '注重内在调养，冥想和静心对身心健康有益。倾听身体的信号，不要忽视潜在的健康问题。',
            relationship: '深层次的精神交流比表面社交更重要。珍惜那些能理解你内心世界的知己。'
        },
        reversed: {
            love: '忽视直觉，压抑真实感受。可能有秘密被隐瞒，缺乏坦诚沟通。',
            career: '过度分析导致犹豫不决，错失良机。或者忽视内在智慧，盲目行动。',
            future: '与内在智慧失去连接，迷失方向。需要重新建立与自我的对话。',
            wealth: '财务信息不透明，可能有隐藏的债务或风险。需要更加警觉和理性。',
            health: '忽视身体信号，压抑情绪影响健康。需要重视心理健康，寻求专业帮助。',
            relationship: '缺乏深度交流，关系流于表面。或者过于封闭，无法与他人建立真实连接。'
        }
    },
    {
        name: '皇后',
        symbol: '👑',
        description: '优雅的皇后坐在丰饶的花园中，周围环绕着成熟的麦田和流水，象征母性与富足。',
        upright: {
            love: '关系充满温暖和滋养，是建立稳定关系的好时期。可能有怀孕或家庭喜事。',
            career: '创意项目会蓬勃发展，团队合作顺利。财务状况改善，收获丰盛。',
            future: '丰盛和富足即将到来，生活会更加美好。关注自我照顾和滋养他人。',
            wealth: '财运旺盛，收入稳定增长。适合投资稳健型项目，耐心培育会带来丰厚回报。',
            health: '身体状况良好，充满生命力。注重营养均衡和自我照顾，保持身心愉悦。',
            relationship: '人际关系和谐，你的温暖和关怀会吸引他人。家庭关系融洽，朋友圈稳定。'
        },
        reversed: {
            love: '过度依赖或控制欲强，关系失衡。可能忽视自我需求，过度付出导致疲惫。',
            career: '创造力受阻，项目停滞不前。可能过于追求完美，或缺乏自信。',
            future: '丰盛被阻碍，可能因过度给予而耗竭。需要学会接受和自我滋养。',
            wealth: '财务管理混乱，过度消费或投资失利。需要更加理性和节制。',
            health: '忽视自我照顾，过度劳累影响健康。需要重视休息和营养补充。',
            relationship: '在关系中失去自我，过度牺牲。或者过于自我中心，忽视他人需求。'
        }
    },
    {
        name: '皇帝',
        symbol: '⚜️',
        description: '威严的皇帝坐在石制王座上，手持权杖，背靠坚固的山脉，代表权威与秩序。',
        upright: {
            love: '需要建立稳定的关系结构，明确彼此的责任和承诺。可能遇到成熟稳重的对象。',
            career: '展现领导力和组织能力的时候到了。建立系统和规则会带来成功。',
            future: '秩序和稳定将主导未来，需要承担更多责任。权威人物可能带来帮助。',
            wealth: '财务管理有序，适合制定长期理财计划。稳健投资和资产配置会带来安全感和收益。',
            health: '建立规律的作息和运动习惯很重要。身体需要纪律和结构化的健康管理。',
            relationship: '在人际关系中展现领导力，但也要注意不要过于强势。建立明确的界限和规则有助于关系稳定。'
        },
        reversed: {
            love: '过于控制或独裁，缺乏灵活性。可能遇到权威型伴侣带来压力，或自己过于强势。',
            career: '滥用权力或缺乏领导力，管理混乱。可能与上级冲突，或无法建立有效系统。',
            future: '过度控制导致僵化，失去灵活性。需要在秩序和自由之间找到平衡。',
            wealth: '财务管理过于严苛或混乱。可能因过度控制错失机会，或缺乏规划导致损失。',
            health: '过于严格的健康管理导致压力，或完全缺乏自律。需要找到适度的平衡。',
            relationship: '人际关系中过于强势或软弱。可能遭遇权威人物的压制，或无法建立健康界限。'
        }
    },
    {
        name: '教皇',
        symbol: '📿',
        description: '神圣的教皇坐在宝座上，手持权杖，两位信徒跪在面前，象征传统与精神指引。',
        upright: {
            love: '传统的恋爱方式更适合，可能走向婚姻。寻求长辈或导师的建议会有帮助。',
            career: '遵循传统方法和规则会更顺利。可能获得导师指导或进入教育培训领域。',
            future: '精神成长和学习的机会即将到来。传统智慧会为你指引方向。',
            wealth: '遵循传统理财方式更为稳妥，可以咨询专业理财顾问。避免高风险投资，稳健为上。',
            health: '传统养生方法对你有益，如中医调理、瑜伽等。寻求专业医疗建议，不要自行诊断。',
            relationship: '尊重传统和礼仪在人际交往中很重要。导师、长辈或精神导师会给予你宝贵的指引。'
        },
        reversed: {
            love: '过于传统或教条，缺乏创新。可能受到外界压力影响感情决定，需要倾听内心。',
            career: '墨守成规，无法适应变化。可能遇到不良导师误导，或过度依赖他人建议。',
            future: '打破传统束缚，寻找新的道路。需要在传统智慧和创新之间找到平衡。',
            wealth: '过于保守错失机会，或盲目听信理财建议。需要建立自己的判断力。',
            health: '过度依赖传统方法而忽视现代医学，或完全拒绝传统智慧。需要综合考虑。',
            relationship: '受到传统观念束缚，无法真实表达。或者完全拒绝他人建议，过于叛逆。'
        }
    },
    {
        name: '恋人',
        symbol: '💕',
        description: '一对恋人站在天使的祝福下，背后是生命之树和智慧之树，象征爱与选择。',
        upright: {
            love: '真爱降临或关系进入新阶段。面临重要的感情选择，跟随内心做决定。',
            career: '需要在多个选项中做出重要决定。合作关系会带来成功。',
            future: '重要的选择即将出现，这个决定会影响未来走向。选择与价值观一致的道路。',
            wealth: '财务上面临选择，需要在多个投资机会中做决定。选择符合你价值观的理财方式。',
            health: '身心和谐很重要，保持生活各方面的平衡。爱与被爱对健康有积极影响。',
            relationship: '重要的人际关系选择，可能是合作伙伴或深度友谊。真诚和价值观一致是关键。'
        },
        reversed: {
            love: '感情选择困难，价值观不一致导致矛盾。可能面临三角关系或背叛。',
            career: '合作关系破裂，难以做出决定。可能选择了错误的道路，需要重新评估。',
            future: '价值观混乱，难以做出正确选择。需要明确自己真正想要什么。',
            wealth: '投资决策失误，可能因贪心选择高风险项目。需要重新审视财务价值观。',
            health: '生活失衡，过度关注某一方面而忽视其他。需要重新找回身心和谐。',
            relationship: '人际关系中缺乏真诚，价值观不合导致冲突。需要寻找真正志同道合的人。'
        }
    },
    {
        name: '战车',
        symbol: '🏇',
        description: '勇士驾驶战车前进，由黑白两只狮身人面兽牵引，象征意志力与控制。',
        upright: {
            love: '主动追求会获得成功，克服障碍赢得芳心。需要平衡感性和理性。',
            career: '全力以赴会取得胜利，项目快速推进。出差或搬迁可能带来机遇。',
            future: '通过意志力和决心，你将克服困难达成目标。快速的进展即将到来。',
            wealth: '财运快速提升，积极进取会带来收益。适合短期投资，但要保持理性控制。',
            health: '精力旺盛，适合挑战性的运动。注意平衡身心，避免过度消耗体力。',
            relationship: '在人际关系中展现自信和决心，但要注意倾听他人意见。团队合作需要协调。'
        },
        reversed: {
            love: '过于强势或失控，感情中缺乏方向。可能因冲动破坏关系。',
            career: '失去控制，项目停滞或方向错误。可能过于激进导致失败。',
            future: '意志力不足或方向迷失。需要重新找回内在的驱动力和明确目标。',
            wealth: '财务管理失控，冲动投资导致损失。需要重新建立理性的理财策略。',
            health: '过度消耗体力，身心失衡。需要放慢节奏，注意休息。',
            relationship: '人际关系中过于强势或软弱，无法有效沟通。需要找到平衡点。'
        }
    },
    {
        name: '力量',
        symbol: '🦁',
        description: '温柔的女子轻抚雄狮的头颅，头顶无限符号，象征以柔克刚的内在力量。',
        upright: {
            love: '用温柔和耐心对待感情，真诚会打动对方。控制情绪，以爱化解矛盾。',
            career: '以柔克刚的方式处理工作挑战。耐心和毅力会带来成功。',
            future: '内在力量将帮助你战胜困难。温和而坚定的态度会带来胜利。',
            wealth: '财务管理需要耐心和自制力。稳健投资，避免冲动消费，长期坚持会有回报。',
            health: '身心健康需要温和的调养。瑜伽、太极等柔和运动很适合。情绪管理对健康很重要。',
            relationship: '以同理心和耐心对待他人，你的温柔会化解冲突。真诚的关怀能赢得人心。'
        },
        reversed: {
            love: '缺乏耐心或过于软弱，无法控制情绪。可能被恐惧或愤怒主导。',
            career: '失去耐心，用强硬方式处理问题。或者过于软弱，无法坚持立场。',
            future: '内在力量被削弱，自我怀疑。需要重新找回自信和勇气。',
            wealth: '缺乏自制力，冲动消费。或者过于谨慎，错失机会。',
            health: '情绪失控影响健康，压力过大。需要学习情绪管理和放松技巧。',
            relationship: '人际关系中缺乏同理心，或过于迁就他人。需要找到温柔与坚定的平衡。'
        }
    },
    {
        name: '隐士',
        symbol: '🕯️',
        description: '孤独的隐士站在雪山之巅，手持明灯照亮前路，象征内在探索与智慧。',
        upright: {
            love: '需要独处和自我反思的时期。单身者适合先提升自己，恋爱中的人需要个人空间。',
            career: '深入研究和独立工作会有成果。寻求内在智慧而非外在建议。',
            future: '独处和反思的时期即将到来。内在探索会带来重要的领悟。',
            wealth: '财务规划需要深思熟虑，独立研究和学习理财知识。避免盲目跟风，寻找适合自己的方式。',
            health: '独处和静心对身心健康有益。适合进行冥想、禅修等内在修炼。定期体检，关注健康。',
            relationship: '暂时减少社交，专注于自我成长。真正的朋友会理解你需要独处的时光。'
        },
        reversed: {
            love: '过度孤立或拒绝独处。可能逃避自我反思，或过于封闭无法建立关系。',
            career: '孤立无援，拒绝寻求帮助。或者过度依赖他人，缺乏独立思考。',
            future: '迷失方向，缺乏内在指引。需要重新建立与自我的连接。',
            wealth: '财务决策缺乏深思熟虑，盲目跟风。或者过于保守，错失机会。',
            health: '过度孤立影响心理健康，或拒绝独处导致疲惫。需要找到平衡。',
            relationship: '社交孤立或过度社交逃避自我。需要在独处和社交之间找到平衡。'
        }
    },
    {
        name: '命运之轮',
        symbol: '☸️',
        description: '巨大的命运之轮在云端旋转，四个神秘生物守护四角，象征生命的循环与变化。',
        upright: {
            love: '感情状况即将发生转变，可能是好的转机。顺应变化，接受命运安排。',
            career: '职业生涯的转折点，机遇降临。把握时机，顺势而为。',
            future: '生活即将迎来重大转变，命运之轮开始转动。变化是必然的，保持灵活。',
            wealth: '财运起伏，可能有意外之财或突然的支出。保持灵活应对，顺应变化调整理财策略。',
            health: '健康状况可能有变化，注意身体信号。生活规律的调整会带来积极影响。',
            relationship: '人际关系进入新的循环，旧友重逢或新朋友出现。因果循环，善待他人会有好报。'
        },
        reversed: {
            love: '感情陷入恶性循环，重复旧的模式。抗拒变化导致停滞。',
            career: '运气不佳，错失机会。或者抗拒必要的改变，导致困境。',
            future: '命运之轮逆转，可能经历低谷。需要接受变化，打破恶性循环。',
            wealth: '财运下滑，投资失利。可能因抗拒变化而错失转机。',
            health: '健康状况恶化，旧疾复发。需要打破不良生活习惯的循环。',
            relationship: '人际关系陷入恶性循环，重复旧的冲突模式。需要主动改变。'
        }
    },
    {
        name: '正义',
        symbol: '⚖️',
        description: '正义女神端坐宝座，一手持剑一手持天平，象征公平、真相与因果。',
        upright: {
            love: '关系需要公平和平衡，诚实面对问题。过去的行为会得到相应的结果。',
            career: '公平的对待和努力会得到应有的回报。法律或合同事务需要注意。',
            future: '因果循环，你的行为会带来相应的结果。公正和真相即将显现。',
            wealth: '财务上要公平交易，诚信经营。合同和法律事务需谨慎处理。付出会得到公正的回报。',
            health: '身体状况是过去生活习惯的反映。公平对待身体，规律作息和饮食会带来健康。',
            relationship: '人际关系需要公平和诚实。你如何对待他人，他人也会如何对待你。保持正直。'
        },
        reversed: {
            love: '关系不公平，一方过度付出。可能面临不公正的对待或自己不够诚实。',
            career: '不公平的待遇，努力未得到认可。或者法律纠纷，合同问题。',
            future: '逃避责任，不愿面对后果。需要承担自己行为的责任。',
            wealth: '财务纠纷，不诚信交易。可能遭遇欺诈或自己不够诚实。',
            health: '忽视身体警告，不愿面对健康问题的真相。需要诚实面对并改变。',
            relationship: '人际关系中不公平或不诚实。可能被不公正对待，或自己缺乏诚信。'
        }
    },
    {
        name: '倒吊人',
        symbol: '🙃',
        description: '一个人倒挂在树上，头顶光环，表情平静，象征牺牲、等待与换位思考。',
        upright: {
            love: '换个角度看待感情问题，可能需要做出牺牲。暂时的等待会带来更好的结果。',
            career: '暂停和重新评估当前状况。从不同视角看问题会有新发现。',
            future: '需要放慢脚步，从新的角度看待生活。短暂的停滞是为了更好的前进。',
            wealth: '财务上需要暂缓行动，重新审视投资策略。短期牺牲可能带来长期收益。',
            health: '放慢节奏，给身体休息的时间。换个角度看待健康问题，可能发现新的解决方案。',
            relationship: '换位思考能改善人际关系。暂时的退让和牺牲会带来更深的理解和连接。'
        },
        reversed: {
            love: '无谓的牺牲，关系停滞不前。拒绝换位思考，固执己见。',
            career: '拖延症，无法做出决定。或者过度牺牲，失去自我。',
            future: '困在旧的思维模式中，拒绝改变视角。需要主动打破僵局。',
            wealth: '财务决策拖延，错失良机。或者盲目牺牲，得不偿失。',
            health: '拒绝休息，过度透支身体。或者过度消极，放弃努力。',
            relationship: '人际关系中过度牺牲或完全不愿妥协。需要找到平衡点。'
        }
    },
    {
        name: '死神',
        symbol: '💀',
        description: '身穿黑袍的死神骑着白马而来，手持黑旗，象征结束、转变与重生。',
        upright: {
            love: '旧的关系模式结束，为新的开始腾出空间。放下过去，拥抱转变。',
            career: '职业生涯的重大转型期，旧工作结束，新机会出现。不要抗拒改变。',
            future: '重大的结束和转变即将发生，这是必要的蜕变。结束意味着新生。',
            wealth: '财务状况面临重大转变，旧的收入来源可能结束，新的机会即将出现。放下执着。',
            health: '旧的健康习惯需要改变，开启新的生活方式。转变虽然艰难，但对健康有益。',
            relationship: '某些人际关系走到尽头，为新的友谊腾出空间。接受自然的离别，迎接新的开始。'
        },
        reversed: {
            love: '抗拒必要的结束，死守无望的关系。恐惧改变导致痛苦延续。',
            career: '拒绝转型，固守旧模式导致停滞。或者过度恐惧失去，无法前进。',
            future: '抗拒转变，困在过去无法前行。需要接受结束，才能迎来新生。',
            wealth: '抗拒财务转型，执着于旧的收入模式。恐惧损失导致更大的损失。',
            health: '拒绝改变不良习惯，健康持续恶化。需要勇敢面对，做出必要的改变。',
            relationship: '无法放下已结束的关系，或恐惧失去而紧抓不放。需要学会放手。'
        }
    },
    {
        name: '节制',
        symbol: '🍷',
        description: '天使站在水边，将水在两个圣杯间倒来倒去，象征平衡、调和与耐心。',
        upright: {
            love: '关系需要平衡和调和，避免极端。耐心和适度会带来和谐。',
            career: '团队合作和资源整合会带来成功。保持平衡，避免过度工作。',
            future: '平衡和和谐即将到来，各方面趋于稳定。耐心等待，时机成熟。',
            wealth: '财务管理需要平衡和节制，避免过度消费或过度节俭。稳健的资产配置是关键。',
            health: '身心平衡很重要，适度运动和休息。饮食均衡，避免极端的健康方式。',
            relationship: '人际关系需要调和与妥协。平衡给予和接受，耐心对待他人会带来和谐。'
        },
        reversed: {
            love: '关系失衡，过度或不足。缺乏耐心，急于求成导致冲突。',
            career: '工作生活失衡，过度工作或懈怠。团队合作不协调，资源浪费。',
            future: '生活各方面失去平衡，过于极端。需要重新找回中庸之道。',
            wealth: '财务管理失衡，过度消费或过度吝啬。缺乏节制导致财务混乱。',
            health: '生活习惯极端，过度运动或完全不动。饮食失衡影响健康。',
            relationship: '人际关系中缺乏妥协，过度给予或索取。需要找到平衡点。'
        }
    },
    {
        name: '恶魔',
        symbol: '😈',
        description: '恶魔坐在宝座上，两个被锁链束缚的人类站在面前，象征诱惑、束缚与阴影。',
        upright: {
            love: '警惕不健康的依赖或诱惑，可能陷入有害的关系模式。需要觉察和解脱。',
            career: '物质诱惑或不良习惯影响工作。意识到束缚，才能获得自由。',
            future: '需要警惕诱惑和束缚，认清限制你的事物。觉察是解脱的第一步。',
            wealth: '警惕物质欲望和过度消费。债务或不良理财习惯可能束缚你。觉察问题是改变的开始。',
            health: '不良生活习惯或成瘾行为影响健康。意识到问题，寻求帮助，才能获得自由。',
            relationship: '警惕有害的人际关系或依赖模式。认清那些消耗你能量的关系，勇敢说不。'
        },
        reversed: {
            love: '开始摆脱不健康的关系，觉察到束缚。或者否认问题，更深陷其中。',
            career: '开始摆脱不良工作习惯，寻求改变。或者完全否认问题的存在。',
            future: '开始觉醒，认清束缚并寻求解脱。或者更深地陷入否认和逃避。',
            wealth: '开始摆脱债务和不良消费习惯。或者否认财务问题，继续沉沦。',
            health: '开始戒除不良习惯，寻求健康生活。或者否认成瘾问题，拒绝帮助。',
            relationship: '开始远离有害关系，重获自由。或者否认关系的毒性，继续被束缚。'
        }
    },
    {
        name: '高塔',
        symbol: '🗼',
        description: '闪电击中高塔，塔顶王冠坠落，人们从塔上跌落，象征突变、崩塌与启示。',
        upright: {
            love: '突然的变故或真相揭露，关系面临考验。破坏是为了重建更真实的关系。',
            career: '突发事件打破现状，可能是裁员或重组。危机中蕴含转机。',
            future: '突然的改变即将发生，旧有结构崩塌。虽然震撼，但会带来新的认知。',
            wealth: '财务上可能有突然的损失或变故。虚假的安全感被打破，重新审视理财方式。',
            health: '突发的健康问题需要重视。这是身体发出的警告，需要立即改变生活方式。',
            relationship: '人际关系中的真相突然揭露，虚假的友谊崩塌。虽然痛苦，但能看清真相。'
        },
        reversed: {
            love: '逃避必要的改变，延长痛苦。或者危机已过，开始重建关系。',
            career: '抗拒变革导致更大危机。或者最坏的时刻已过，开始恢复。',
            future: '恐惧改变，紧抓崩塌的结构。或者灾难被避免，有机会重新开始。',
            wealth: '财务危机延续或逐渐恢复。需要从教训中学习，重建财务基础。',
            health: '忽视健康警告，危机加剧。或者开始康复，从疾病中吸取教训。',
            relationship: '拒绝面对关系真相，或开始从破裂中恢复。需要时间疗愈和重建信任。'
        }
    },
    {
        name: '星星',
        symbol: '⭐',
        description: '裸身女子跪在水边，将水倒入池塘和大地，头顶闪耀八芒星，象征希望与灵感。',
        upright: {
            love: '充满希望和理想的恋情，心灵的连接。疗愈过去的伤痛，重拾信心。',
            career: '理想和目标清晰，充满希望和灵感。坚持梦想会实现。',
            future: '希望之光照耀前路，美好的未来在等待。保持信念，梦想会实现。',
            wealth: '财运逐渐好转，保持希望和信念。长期投资和理想的理财规划会带来回报。',
            health: '身心开始疗愈，充满希望。保持积极心态，健康状况会逐步改善。',
            relationship: '人际关系充满希望，真诚的友谊带来疗愈。你的理想会吸引志同道合的人。'
        },
        reversed: {
            love: '失去希望，对爱情感到绝望。理想破灭，需要重新找回信念。',
            career: '缺乏方向和灵感，梦想遥不可及。需要重新点燃内心的希望之光。',
            future: '失去信念，对未来感到迷茫。需要疗愈内心，重建希望。',
            wealth: '财务状况令人失望，投资未达预期。不要放弃，重新调整策略。',
            health: '疗愈受阻，失去信心。需要寻求支持，重新建立积极心态。',
            relationship: '人际关系令人失望，感到孤独。需要疗愈过去的伤痛，重新敞开心扉。'
        }
    },
    {
        name: '月亮',
        symbol: '🌜',
        description: '满月高悬，狼和狗对月嚎叫，龙虾从水中爬出，象征幻觉、恐惧与潜意识。',
        upright: {
            love: '感情中存在不确定性或误解，需要澄清真相。直觉和梦境会给予提示。',
            career: '情况不明朗，可能有隐藏的信息。相信直觉，小心欺骗。',
            future: '迷雾和不确定性笼罩，需要面对内心的恐惧。真相会逐渐浮现。',
            wealth: '财务状况不明朗，可能有隐藏的风险。谨慎投资，避免被表象迷惑。',
            health: '注意心理健康，焦虑和恐惧可能影响身体。倾听潜意识的信号，面对内心恐惧。',
            relationship: '人际关系中可能有误解或隐瞒。相信直觉，但也要澄清事实，避免猜疑。'
        },
        reversed: {
            love: '幻觉消散，真相逐渐清晰。或者更深陷于恐惧和误解中。',
            career: '隐藏的信息被揭露，欺骗被识破。或者继续被迷雾笼罩，无法看清。',
            future: '恐惧开始消退，迷雾散去。或者被焦虑和幻想完全控制。',
            wealth: '财务真相浮现，风险被识别。或者继续被虚假信息误导。',
            health: '心理问题得到重视和治疗。或者焦虑加剧，影响身心健康。',
            relationship: '误解被澄清，真相大白。或者猜疑加深，关系更加混乱。'
        }
    },
    {
        name: '太阳',
        symbol: '☀️',
        description: '灿烂的太阳照耀大地，孩童骑着白马欢笑，向日葵盛开，象征成功与喜悦。',
        upright: {
            love: '充满喜悦和活力的恋情，关系光明美好。真诚和快乐是关键。',
            career: '成功和认可即将到来，事业蒸蒸日上。充满活力和正能量。',
            future: '光明和成功即将到来，一切都会好起来。充满喜悦和生命力的时期。',
            wealth: '财运亨通，收入增加。投资获利，财务状况光明。享受成功的果实。',
            health: '身心健康，充满活力和生命力。阳光和运动对健康有益，保持积极心态。',
            relationship: '人际关系和谐愉快，充满欢笑和温暖。你的正能量会吸引美好的友谊。'
        },
        reversed: {
            love: '快乐被阴影遮蔽，关系中缺乏真诚。过度乐观或虚假的快乐。',
            career: '成功延迟，信心受挫。或者过度自信导致失误。',
            future: '乐观受阻，暂时的阴霾。需要重新找回内在的光明和喜悦。',
            wealth: '财务收益不如预期，或过度消费。需要更加务实和谨慎。',
            health: '活力下降，缺乏阳光和运动。需要重新激发生命力。',
            relationship: '人际关系中缺乏真诚的快乐，表面和谐。需要建立真实的连接。'
        }
    },
    {
        name: '审判',
        symbol: '📯',
        description: '天使吹响号角，逝者从棺材中复活，象征觉醒、更新与重生。',
        upright: {
            love: '重新评估关系，可能是复合或重新开始。宽恕和释怀带来新生。',
            career: '职业生涯的重要评估期，可能获得晋升或认可。过去的努力得到回报。',
            future: '觉醒和重生的时刻到来，重新审视人生。重要的决定会改变未来。',
            wealth: '财务状况得到重新评估，过去的投资得到回报。清理旧账，开启新的理财阶段。',
            health: '身体的觉醒和更新，过去的健康问题得到解决。重新审视生活方式，做出改变。',
            relationship: '人际关系的重新评估，宽恕过去的伤害。旧友重逢，关系得到更新和升华。'
        },
        reversed: {
            love: '拒绝反思关系，无法宽恕。错失复合或新开始的机会。',
            career: '逃避评估，不愿面对职业现状。或者评判过于严苛，自我否定。',
            future: '拒绝觉醒，逃避必要的改变。无法从过去中学习和成长。',
            wealth: '不愿面对财务真相，逃避清算。或者过度自责，无法前进。',
            health: '拒绝改变生活方式，忽视身体警告。或者过度自责影响康复。',
            relationship: '无法宽恕，困在过去的伤害中。或者过度自责，无法重建关系。'
        }
    },
    {
        name: '世界',
        symbol: '🌍',
        description: '舞者在月桂花环中央起舞，四个神秘生物守护四角，象征完成、成就与圆满。',
        upright: {
            love: '关系达到圆满和完整，可能是结婚或达成重要里程碑。幸福美满。',
            career: '项目成功完成，达成重要成就。可能有海外机会或重大成功。',
            future: '圆满和成就即将到来，一个重要周期完成。成功和满足感充盈。',
            wealth: '财务目标达成，经济状况圆满。长期投资获得丰厚回报，财富自由在望。',
            health: '身心达到最佳状态，健康圆满。生活各方面和谐平衡，充满满足感。',
            relationship: '人际关系圆满和谐，社交圈完整。可能有国际友谊或跨文化交流的机会。'
        },
        reversed: {
            love: '关系未达圆满，仍有未完成的课题。或者害怕承诺，逃避完整的关系。',
            career: '项目未完成，目标未达成。或者成功后感到空虚，缺乏新目标。',
            future: '周期未完成，仍有未了的事。或者害怕结束，抗拒新的开始。',
            wealth: '财务目标未达成，或达成后缺乏满足感。需要重新审视真正的财富意义。',
            health: '身心未达到理想状态，仍需努力。或者过度追求完美，反而失去平衡。',
            relationship: '社交圈不完整，缺乏归属感。或者过度追求完美关系，错失真实连接。'
        }
    }
];

// 全局变量
let currentSpread = '';
let selectedCards = [];
let cardsToSelect = 0;
let cutCard = null;
let cutCardReversed = false;
let shuffledDeck = [];
let cardOrientations = []; // 存储每张牌的正逆位状态

// DOM元素
const intro = document.getElementById('intro');
const shuffleArea = document.getElementById('shuffleArea');
const cutArea = document.getElementById('cutArea');
const readingArea = document.getElementById('readingArea');
const spreadTitle = document.getElementById('spreadTitle');
const cardsContainer = document.getElementById('cardsContainer');
const result = document.getElementById('result');
const resultContent = document.getElementById('resultContent');
const cutCardDisplay = document.getElementById('cutCardDisplay');
const cutCardContent = document.getElementById('cutCardContent');
const restartBtn = document.getElementById('restartBtn');

// 初始化事件监听
document.querySelectorAll('.spread-btn').forEach(btn => {
    btn.addEventListener('click', () => {
        currentSpread = btn.dataset.spread;
        startReading();
    });
});

restartBtn.addEventListener('click', restart);

// 开始占卜 - 进入洗牌阶段
function startReading() {
    intro.classList.add('hidden');
    shuffleArea.classList.remove('hidden');
    result.classList.add('hidden');
    selectedCards = [];
    cutCard = null;
    cutCardReversed = false;
    cardOrientations = [];
    
    // 根据占卜类型设置卡牌数量
    if (currentSpread === 'random') {
        cardsToSelect = 1;
    } else {
        cardsToSelect = 3;
    }
    
    // 开始洗牌动画
    shuffleCards();
}

// 洗牌动画
function shuffleCards() {
    const shuffleCardsContainer = document.getElementById('shuffleCards');
    shuffleCardsContainer.innerHTML = '';
    
    // 播放洗牌音效
    playSound('shuffle');
    
    // 创建多张卡牌进行洗牌动画
    for (let i = 0; i < 15; i++) {
        const card = document.createElement('div');
        card.className = 'shuffle-card';
        card.style.animationDelay = `${i * 0.1}s`;
        shuffleCardsContainer.appendChild(card);
        
        // 每隔一段时间播放洗牌音效
        if (i % 3 === 0) {
            setTimeout(() => playSound('shuffle'), i * 100);
        }
    }
    
    // 洗牌3秒后进入切牌阶段
    setTimeout(() => {
        shuffledDeck = [...tarotCards].sort(() => Math.random() - 0.5);
        shuffleArea.classList.add('hidden');
        cutArea.classList.remove('hidden');
        initCutDeck();
        playSound('complete');
    }, 3000);
}

// 初始化切牌
function initCutDeck() {
    const deckParts = document.querySelectorAll('.deck-part');
    deckParts.forEach(part => {
        part.addEventListener('click', () => {
            playSound('select');
            performCut(part.dataset.position);
        });
    });
}

// 执行切牌
function performCut(position) {
    // 根据切牌位置选择切牌
    let cutIndex;
    switch(position) {
        case 'left':
            cutIndex = Math.floor(shuffledDeck.length * 0.25);
            break;
        case 'middle':
            cutIndex = Math.floor(shuffledDeck.length * 0.5);
            break;
        case 'right':
            cutIndex = Math.floor(shuffledDeck.length * 0.75);
            break;
    }
    
    // 切牌是被切出部分的最底部那张
    cutCard = shuffledDeck[cutIndex];
    // 随机决定切牌的正逆位
    cutCardReversed = Math.random() < 0.5;
    
    // 切牌动画
    const deckParts = document.querySelectorAll('.deck-part');
    deckParts.forEach(part => {
        if (part.dataset.position === position) {
            part.style.transform = 'translateY(-20px)';
            part.style.opacity = '0.5';
        }
    });
    
    // 1秒后进入抽牌阶段
    setTimeout(() => {
        cutArea.classList.add('hidden');
        readingArea.classList.remove('hidden');
        setSpreadTitle();
        createCardDeck();
    }, 1000);
}

// 设置牌阵标题
function setSpreadTitle() {
    switch(currentSpread) {
        case 'love':
            spreadTitle.textContent = '爱情占卜 - 请抽取三张牌';
            break;
        case 'career':
            spreadTitle.textContent = '事业占卜 - 请抽取三张牌';
            break;
        case 'future':
            spreadTitle.textContent = '未来预示 - 请抽取三张牌';
            break;
        case 'wealth':
            spreadTitle.textContent = '财运占卜 - 请抽取三张牌';
            break;
        case 'health':
            spreadTitle.textContent = '健康占卜 - 请抽取三张牌';
            break;
        case 'relationship':
            spreadTitle.textContent = '人际关系 - 请抽取三张牌';
            break;
        case 'random':
            spreadTitle.textContent = '随机指引 - 请抽取一张牌';
            break;
    }
}

// 创建卡牌 - 扇形摊开
function createCardDeck() {
    cardsContainer.innerHTML = '';
    
    // 使用已洗好的牌
    const cardsToShow = shuffledDeck.slice(0, 10); // 显示10张供选择
    
    cardsToShow.forEach((card, index) => {
        const cardElement = document.createElement('div');
        cardElement.className = 'tarot-card';
        cardElement.dataset.cardIndex = index;
        cardElement.innerHTML = `
            <div class="card-inner">
                <div class="card-back-face"></div>
                <div class="card-front">
                    <div class="card-symbol">${card.symbol}</div>
                    <div class="card-name">${card.name}</div>
                </div>
            </div>
        `;
        
        cardElement.addEventListener('click', () => selectCard(cardElement, card, index));
        cardsContainer.appendChild(cardElement);
        
        // 添加延迟动画
        setTimeout(() => {
            cardElement.style.animation = 'fadeIn 0.5s ease';
            cardElement.style.opacity = '1';
        }, index * 80);
    });
}

// 选择卡牌
function selectCard(cardElement, card, index) {
    if (cardElement.classList.contains('flipped')) return;
    if (selectedCards.length >= cardsToSelect) return;
    
    // 播放翻牌音效
    playSound('flip');
    
    // 翻牌
    cardElement.classList.add('flipped');
    selectedCards.push(card);
    
    // 随机决定正位或逆位（50%概率）
    const isReversed = Math.random() < 0.5;
    cardOrientations.push(isReversed);
    
    // 添加选中标记
    setTimeout(() => {
        cardElement.style.transform = 'scale(1.1)';
    }, 300);
    
    // 如果已选够卡牌，显示结果
    if (selectedCards.length === cardsToSelect) {
        setTimeout(() => {
            playSound('complete');
            showResult();
        }, 1200);
    }
}

// 显示结果
function showResult() {
    // 先显示切牌
    if (cutCard) {
        cutCardDisplay.classList.remove('hidden');
        const cutMeaning = getCutCardMeaning(cutCard);
        const cutOrientationText = cutCardReversed ? ' [逆位]' : ' [正位]';
        const cutReversedStyle = cutCardReversed ? 'style="transform: rotate(180deg); display: inline-block;"' : '';
        
        cutCardContent.innerHTML = `
            <div class="card-result">
                <h4><span ${cutReversedStyle}>${cutCard.symbol}</span> ${cutCard.name} ${cutOrientationText}</h4>
                <p>${cutMeaning}</p>
                <p style="margin-top: 15px; font-style: italic; opacity: 0.9;">
                    这张切牌反映了你对这个问题的潜在心态和能量状态。
                </p>
            </div>
        `;
    }
    
    // 显示抽到的牌
    result.classList.remove('hidden');
    resultContent.innerHTML = '';
    
    selectedCards.forEach((card, index) => {
        const cardResult = document.createElement('div');
        cardResult.className = 'card-result';
        cardResult.style.animationDelay = `${index * 0.2}s`;
        
        const { position, meaning, description, isReversed } = getCardMeaning(card, index);
        
        // 如果是逆位，添加特殊样式
        const reversedStyle = isReversed ? 'style="transform: rotate(180deg); display: inline-block;"' : '';
        const reversedClass = isReversed ? 'reversed-card' : '';
        
        cardResult.innerHTML = `
            <div class="card-position">${position}</div>
            <h4 class="${reversedClass}"><span ${reversedStyle}>${card.symbol}</span> ${card.name}</h4>
            <p class="card-description">${description}</p>
            <p class="card-meaning">${meaning}</p>
        `;
        
        resultContent.appendChild(cardResult);
    });
    
    // 添加总结
    const summary = document.createElement('div');
    summary.className = 'card-result';
    summary.style.animationDelay = `${selectedCards.length * 0.2}s`;
    summary.innerHTML = `
        <h4>✨ 占卜总结</h4>
        <p>${getSummary()}</p>
    `;
    resultContent.appendChild(summary);
    
    // 滚动到结果
    setTimeout(() => {
        result.scrollIntoView({ behavior: 'smooth', block: 'start' });
    }, 500);
}

// 获取切牌的含义
function getCutCardMeaning(card) {
    const orientation = cutCardReversed ? 'reversed' : 'upright';
    let meaning = '';
    
    switch(currentSpread) {
        case 'love':
            meaning = card[orientation].love;
            break;
        case 'career':
            meaning = card[orientation].career;
            break;
        case 'future':
            meaning = card[orientation].future;
            break;
        case 'wealth':
            meaning = card[orientation].wealth;
            break;
        case 'health':
            meaning = card[orientation].health;
            break;
        case 'relationship':
            meaning = card[orientation].relationship;
            break;
        default:
            meaning = card[orientation].love;
    }
    
    return meaning;
}

// 获取卡牌含义和位置
function getCardMeaning(card, index) {
    let position = '';
    let meaning = '';
    let description = card.description || '';
    const isReversed = cardOrientations[index] || false;
    const orientation = isReversed ? 'reversed' : 'upright';
    
    // 添加正逆位标识到位置
    const orientationText = isReversed ? ' [逆位]' : ' [正位]';
    
    switch(currentSpread) {
        case 'love':
            const lovePositions = ['💕 爱情运势', '💼 事业影响', '🔮 未来发展'];
            position = lovePositions[index] + orientationText;
            meaning = index === 0 ? card[orientation].love : (index === 1 ? card[orientation].career : card[orientation].future);
            break;
        case 'career':
            const careerPositions = ['💼 事业运势', '💕 感情影响', '🔮 未来发展'];
            position = careerPositions[index] + orientationText;
            meaning = index === 0 ? card[orientation].career : (index === 1 ? card[orientation].love : card[orientation].future);
            break;
        case 'future':
            const futurePositions = ['🔮 即将发生', '💕 爱情方面', '💼 事业方面'];
            position = futurePositions[index] + orientationText;
            meaning = index === 0 ? card[orientation].future : (index === 1 ? card[orientation].love : card[orientation].career);
            break;
        case 'wealth':
            const wealthPositions = ['💰 财运状况', '💼 事业影响', '🔮 未来趋势'];
            position = wealthPositions[index] + orientationText;
            meaning = index === 0 ? card[orientation].wealth : (index === 1 ? card[orientation].career : card[orientation].future);
            break;
        case 'health':
            const healthPositions = ['🌿 健康状况', '💕 情绪影响', '🔮 未来建议'];
            position = healthPositions[index] + orientationText;
            meaning = index === 0 ? card[orientation].health : (index === 1 ? card[orientation].love : card[orientation].future);
            break;
        case 'relationship':
            const relationshipPositions = ['🤝 人际运势', '💼 工作关系', '🔮 未来发展'];
            position = relationshipPositions[index] + orientationText;
            meaning = index === 0 ? card[orientation].relationship : (index === 1 ? card[orientation].career : card[orientation].future);
            break;
        case 'random':
            position = '✨ 今日指引' + orientationText;
            // 随机选择一个维度的解释
            const aspects = ['love', 'career', 'future', 'wealth', 'health', 'relationship'];
            const randomAspect = aspects[Math.floor(Math.random() * aspects.length)];
            meaning = card[orientation][randomAspect];
            
            // 添加额外的综合指引
            const guidance = getRandomGuidance(card);
            meaning = `${meaning}\n\n${guidance}`;
            break;
    }
    
    return { position, meaning, description, isReversed };
}

// 获取随机指引的额外建议
function getRandomGuidance(card) {
    const guidances = [
        `💡 建议：静心思考这张牌带给你的启示，它可能正是你当下最需要的指引。`,
        `🌟 提示：这张牌的出现不是偶然，它反映了你内心深处的某种状态或即将到来的机遇。`,
        `✨ 启发：将这张牌的含义与你当前的生活联系起来，你会发现意想不到的答案。`,
        `🔮 指引：塔罗牌是一面镜子，它映照出你内心的真实想法。相信你的直觉。`,
        `💫 洞察：这张牌为你揭示了一个重要的方向，无论是爱情、事业还是人生，都值得深思。`
    ];
    
    return guidances[Math.floor(Math.random() * guidances.length)];
}

// 获取总结
function getSummary() {
    const summaries = {
        love: '这三张牌从爱情、事业、未来三个维度为你揭示了完整的运势。爱情需要用心经营，事业会影响感情发展，而未来掌握在你手中。记住，真爱需要双方的努力和理解。',
        career: '这三张牌从事业、感情、未来三个角度为你指明方向。事业的发展需要平衡生活各方面，感情状态会影响工作表现，而你的选择将塑造未来。保持专注，同时不要忽视生活的其他面向。',
        future: '这三张牌预示了即将发生的事情，以及对爱情和事业的影响。未来充满可能性，你的态度和行动会决定结果。保持开放的心态，积极面对即将到来的变化。',
        wealth: '这三张牌从财运、事业、未来三个角度为你揭示财务状况。财富的积累需要智慧和耐心，事业发展直接影响收入，而长远规划决定财务自由。记住，金钱是工具，不是目的，合理理财才能带来真正的富足。',
        health: '这三张牌从健康、情绪、未来三个维度为你指引养生之道。身心健康是一切的基础，情绪状态影响身体机能，而良好的生活习惯决定未来的健康。倾听身体的声音，保持身心平衡。',
        relationship: '这三张牌从人际关系、工作关系、未来发展三个角度为你揭示社交运势。真诚和善意是人际交往的基础，职场关系需要智慧经营，而你对待他人的方式将塑造未来的人脉。记住，良好的关系需要用心维护。',
        random: '当你感到迷茫时，塔罗牌为你抽取了这张指引。它可能关于爱情、事业、财运、健康或人际关系，但最重要的是，它反映了你当下的能量状态。静心聆听内心的声音，答案就在你心中。记住，塔罗牌不是预言未来，而是帮助你更好地认识自己，做出明智的选择。'
    };
    
    return summaries[currentSpread] || '愿这次占卜能为你带来启发和指引。';
}

// 重新开始
function restart() {
    readingArea.classList.add('hidden');
    shuffleArea.classList.add('hidden');
    cutArea.classList.add('hidden');
    cutCardDisplay.classList.add('hidden');
    intro.classList.remove('hidden');
    selectedCards = [];
    cutCard = null;
    cutCardReversed = false;
    cardOrientations = [];
    currentSpread = '';
    shuffledDeck = [];
}

// 添加页面加载动画
window.addEventListener('load', () => {
    document.body.style.opacity = '0';
    setTimeout(() => {
        document.body.style.transition = 'opacity 1s ease';
        document.body.style.opacity = '1';
    }, 100);
});

// ==================== 新功能：音效系统 ====================
const soundEnabled = localStorage.getItem('soundEnabled') !== 'false';
let isSoundOn = soundEnabled;

// 音效对象（使用Web Audio API生成简单音效）
const AudioContext = window.AudioContext || window.webkitAudioContext;
const audioContext = AudioContext ? new AudioContext() : null;

function playSound(type) {
    if (!isSoundOn || !audioContext) return;
    
    const oscillator = audioContext.createOscillator();
    const gainNode = audioContext.createGain();
    
    oscillator.connect(gainNode);
    gainNode.connect(audioContext.destination);
    
    switch(type) {
        case 'shuffle':
            // 洗牌音效 - 快速的音符序列
            oscillator.frequency.value = 400;
            oscillator.type = 'sine';
            gainNode.gain.setValueAtTime(0.1, audioContext.currentTime);
            gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 0.1);
            oscillator.start(audioContext.currentTime);
            oscillator.stop(audioContext.currentTime + 0.1);
            break;
            
        case 'flip':
            // 翻牌音效 - 清脆的声音
            oscillator.frequency.value = 800;
            oscillator.type = 'triangle';
            gainNode.gain.setValueAtTime(0.15, audioContext.currentTime);
            gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 0.2);
            oscillator.start(audioContext.currentTime);
            oscillator.stop(audioContext.currentTime + 0.2);
            break;
            
        case 'select':
            // 选择音效 - 柔和的提示音
            oscillator.frequency.value = 600;
            oscillator.type = 'sine';
            gainNode.gain.setValueAtTime(0.1, audioContext.currentTime);
            gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 0.15);
            oscillator.start(audioContext.currentTime);
            oscillator.stop(audioContext.currentTime + 0.15);
            break;
            
        case 'complete':
            // 完成音效 - 上升的音符
            oscillator.frequency.setValueAtTime(400, audioContext.currentTime);
            oscillator.frequency.exponentialRampToValueAtTime(800, audioContext.currentTime + 0.3);
            oscillator.type = 'sine';
            gainNode.gain.setValueAtTime(0.15, audioContext.currentTime);
            gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 0.3);
            oscillator.start(audioContext.currentTime);
            oscillator.stop(audioContext.currentTime + 0.3);
            break;
    }
}

// 音效开关按钮
const soundToggle = document.getElementById('soundToggle');
if (soundToggle) {
    soundToggle.textContent = isSoundOn ? '🔊' : '🔇';
    if (!isSoundOn) {
        soundToggle.classList.add('muted');
    }
    
    soundToggle.addEventListener('click', () => {
        isSoundOn = !isSoundOn;
        localStorage.setItem('soundEnabled', isSoundOn);
        soundToggle.textContent = isSoundOn ? '🔊' : '🔇';
        
        if (isSoundOn) {
            soundToggle.classList.remove('muted');
            playSound('select');
        } else {
            soundToggle.classList.add('muted');
        }
    });
}

// ==================== 新功能：页面导航 ====================
const navLinks = document.querySelectorAll('.nav-link');
const pages = {
    divination: document.getElementById('divinationPage'),
    gallery: document.getElementById('galleryPage'),
    history: document.getElementById('historyPage'),
    yesno: document.getElementById('yesnoPage')
};

// 添加每日一牌页面到pages对象
pages.daily = document.getElementById('dailyPage');

// 页面切换功能
navLinks.forEach(link => {
    link.addEventListener('click', (e) => {
        e.preventDefault();
        const targetPage = link.dataset.page;
        
        playSound('select');
        
        // 更新导航激活状态
        navLinks.forEach(l => l.classList.remove('active'));
        link.classList.add('active');
        
        // 切换页面显示
        Object.keys(pages).forEach(key => {
            if (key === targetPage) {
                pages[key].classList.remove('hidden');
            } else {
                pages[key].classList.add('hidden');
            }
        });
        
        // 如果切换到图鉴页面，生成图鉴
        if (targetPage === 'gallery') {
            generateGallery();
        }
        
        // 如果切换到历史页面，加载历史
        if (targetPage === 'history') {
            loadHistory();
        }
        
        // 如果切换到每日一牌页面，初始化
        if (targetPage === 'daily') {
            initDailyCard();
        }
    });
});

// ==================== 新功能：问题输入 ====================
const questionInput = document.getElementById('questionInput');
let userQuestion = '';

// 保存用户问题
if (questionInput) {
    questionInput.addEventListener('input', (e) => {
        userQuestion = e.target.value;
    });
}

// ==================== 新功能：塔罗牌图鉴 ====================
const galleryGrid = document.getElementById('galleryGrid');
const filterBtns = document.querySelectorAll('.filter-btn');

function generateGallery(filter = 'all') {
    if (!galleryGrid) return;
    
    galleryGrid.innerHTML = '';
    
    tarotCards.forEach(card => {
        const cardElement = document.createElement('div');
        cardElement.className = 'gallery-card';
        
        // 根据筛选显示正位或逆位
        let meaningHTML = '';
        
        if (filter === 'all' || filter === 'upright') {
            meaningHTML += `
                <div class="meaning-section">
                    <div class="meaning-title">正位 - 爱情</div>
                    <div class="meaning-text">${card.upright.love}</div>
                </div>
            `;
        }
        
        if (filter === 'all' || filter === 'reversed') {
            meaningHTML += `
                <div class="meaning-section">
                    <div class="meaning-title">逆位 - 爱情</div>
                    <div class="meaning-text">${card.reversed.love}</div>
                </div>
            `;
        }
        
        cardElement.innerHTML = `
            <div class="gallery-card-symbol">${card.symbol}</div>
            <div class="gallery-card-name">${card.name}</div>
            <div class="gallery-card-desc">${card.description}</div>
            <div class="gallery-card-meanings">${meaningHTML}</div>
        `;
        
        galleryGrid.appendChild(cardElement);
    });
}

// 图鉴筛选功能
filterBtns.forEach(btn => {
    btn.addEventListener('click', () => {
        filterBtns.forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        generateGallery(btn.dataset.filter);
    });
});

// ==================== 新功能：历史记录 ====================
function saveToHistory(spreadType, question, cards, orientations) {
    const history = JSON.parse(localStorage.getItem('tarotHistory') || '[]');
    
    const record = {
        id: Date.now(),
        date: new Date().toLocaleString('zh-CN'),
        type: spreadType,
        question: question || '未输入问题',
        cards: cards.map((card, index) => ({
            name: card.name,
            symbol: card.symbol,
            isReversed: orientations[index] || false,
            position: getPositionName(spreadType, index)
        }))
    };
    
    history.unshift(record);
    
    // 只保留最近50条记录
    if (history.length > 50) {
        history.splice(50);
    }
    
    localStorage.setItem('tarotHistory', JSON.stringify(history));
}

function getPositionName(spreadType, index) {
    const positions = {
        love: ['爱情运势', '事业影响', '未来发展'],
        career: ['事业运势', '感情影响', '未来发展'],
        future: ['即将发生', '爱情方面', '事业方面'],
        wealth: ['财运状况', '事业影响', '未来趋势'],
        health: ['健康状况', '情绪影响', '未来建议'],
        relationship: ['人际运势', '工作关系', '未来发展'],
        random: ['今日指引']
    };
    
    return positions[spreadType]?.[index] || '未知位置';
}

function loadHistory() {
    const historyList = document.getElementById('historyList');
    if (!historyList) return;
    
    const history = JSON.parse(localStorage.getItem('tarotHistory') || '[]');
    
    if (history.length === 0) {
        historyList.innerHTML = `
            <div class="empty-history">
                <p>📭 暂无占卜记录</p>
                <p class="empty-tip">开始你的第一次占卜吧！</p>
            </div>
        `;
        return;
    }
    
    historyList.innerHTML = history.map(record => `
        <div class="history-item">
            <div class="history-header">
                <div class="history-type">${getSpreadTypeName(record.type)}</div>
                <div class="history-date">${record.date}</div>
            </div>
            ${record.question !== '未输入问题' ? `
                <div class="history-question">
                    💭 ${record.question}
                </div>
            ` : ''}
            <div class="history-cards">
                ${record.cards.map(card => `
                    <div class="history-card-item">
                        <div class="history-card-name">
                            ${card.isReversed ? '🔄 ' : ''}${card.symbol} ${card.name} 
                            ${card.isReversed ? '[逆位]' : '[正位]'}
                        </div>
                        <div>${card.position}</div>
                    </div>
                `).join('')}
            </div>
        </div>
    `).join('');
}

function getSpreadTypeName(type) {
    const names = {
        love: '💕 爱情占卜',
        career: '💼 事业占卜',
        future: '🔮 未来预示',
        wealth: '💰 财运占卜',
        health: '🌿 健康占卜',
        relationship: '🤝 人际关系',
        random: '✨ 随机指引',
        yesno: '🎯 是/否占卜'
    };
    return names[type] || type;
}

// 清空历史记录
const clearHistoryBtn = document.getElementById('clearHistoryBtn');
if (clearHistoryBtn) {
    clearHistoryBtn.addEventListener('click', () => {
        if (confirm('确定要清空所有历史记录吗？')) {
            localStorage.removeItem('tarotHistory');
            loadHistory();
        }
    });
}

// ==================== 新功能：是/否占卜 ====================
const yesnoDrawBtn = document.getElementById('yesnoDrawBtn');
const yesnoQuestion = document.getElementById('yesnoQuestion');
const yesnoResult = document.getElementById('yesnoResult');
const yesnoCard = document.getElementById('yesnoCard');
const yesnoAnswer = document.getElementById('yesnoAnswer');
const yesnoExplanation = document.getElementById('yesnoExplanation');
const yesnoRestartBtn = document.getElementById('yesnoRestartBtn');

if (yesnoDrawBtn) {
    yesnoDrawBtn.addEventListener('click', () => {
        const question = yesnoQuestion.value.trim();
        
        if (!question) {
            alert('请先输入你的问题！');
            return;
        }
        
        performYesNoReading(question);
    });
}

if (yesnoRestartBtn) {
    yesnoRestartBtn.addEventListener('click', () => {
        yesnoResult.classList.add('hidden');
        yesnoQuestion.value = '';
    });
}

function performYesNoReading(question) {
    // 播放翻牌音效
    playSound('flip');
    
    // 随机抽取一张牌
    const randomCard = tarotCards[Math.floor(Math.random() * tarotCards.length)];
    const isReversed = Math.random() < 0.5;
    
    // 根据牌的含义判断是/否
    const answer = getYesNoAnswer(randomCard, isReversed);
    
    // 显示结果
    yesnoCard.textContent = randomCard.symbol;
    yesnoAnswer.textContent = answer.text;
    yesnoAnswer.className = `yesno-answer ${answer.type}`;
    
    const orientation = isReversed ? 'reversed' : 'upright';
    yesnoExplanation.innerHTML = `
        <h4>${randomCard.name} ${isReversed ? '[逆位]' : '[正位]'}</h4>
        <p>${randomCard.description}</p>
        <p style="margin-top: 15px;"><strong>解读：</strong>${randomCard[orientation].future}</p>
        <p style="margin-top: 10px; font-style: italic; opacity: 0.9;">
            ${answer.explanation}
        </p>
    `;
    
    yesnoResult.classList.remove('hidden');
    
    // 播放完成音效
    setTimeout(() => playSound('complete'), 500);
    
    // 保存到历史
    saveToHistory('yesno', question, [randomCard], [isReversed]);
}

function getYesNoAnswer(card, isReversed) {
    // 正位倾向于"是"，逆位倾向于"否"
    // 但某些牌有特殊含义
    
    const positiveCards = ['愚者', '魔术师', '皇后', '皇帝', '恋人', '战车', '力量', '命运之轮', '太阳', '世界'];
    const negativeCards = ['死神', '恶魔', '高塔', '月亮'];
    const neutralCards = ['女祭司', '教皇', '隐士', '倒吊人', '节制', '星星', '审判'];
    
    if (!isReversed) {
        // 正位
        if (positiveCards.includes(card.name)) {
            return {
                type: 'yes',
                text: '是 (YES)',
                explanation: '塔罗牌显示积极的能量，答案倾向于肯定。'
            };
        } else if (negativeCards.includes(card.name)) {
            return {
                type: 'no',
                text: '否 (NO)',
                explanation: '塔罗牌显示需要谨慎，答案倾向于否定。'
            };
        } else {
            return {
                type: 'maybe',
                text: '可能 (MAYBE)',
                explanation: '塔罗牌显示情况复杂，需要更多思考和准备。'
            };
        }
    } else {
        // 逆位
        if (positiveCards.includes(card.name)) {
            return {
                type: 'maybe',
                text: '可能 (MAYBE)',
                explanation: '正面的牌逆位，表示有阻碍但并非完全否定，需要克服困难。'
            };
        } else if (negativeCards.includes(card.name)) {
            return {
                type: 'yes',
                text: '是 (YES)',
                explanation: '负面的牌逆位，表示困难正在消退，答案倾向于肯定。'
            };
        } else {
            return {
                type: 'no',
                text: '否 (NO)',
                explanation: '塔罗牌逆位显示能量受阻，现在不是好时机。'
            };
        }
    }
}

// ==================== 修改原有的showResult函数，添加历史记录保存 ====================
// 找到原来的showResult函数，在最后添加保存历史的代码
const originalShowResult = showResult;
showResult = function() {
    originalShowResult.call(this);
    
    // 保存到历史记录
    saveToHistory(currentSpread, userQuestion, selectedCards, cardOrientations);
    
    // 清空问题输入
    if (questionInput) {
        questionInput.value = '';
        userQuestion = '';
    }
};

// ==================== 新功能：每日一牌 ====================
const dailyDrawBtn = document.getElementById('dailyDrawBtn');
const dailyCardBack = document.getElementById('dailyCardBack');
const dailyResult = document.getElementById('dailyResult');
const dailyDate = document.getElementById('dailyDate');
const dailyCardSymbol = document.getElementById('dailyCardSymbol');
const dailyCardName = document.getElementById('dailyCardName');
const dailyCardOrientation = document.getElementById('dailyCardOrientation');
const dailyCardDescription = document.getElementById('dailyCardDescription');
const dailyLove = document.getElementById('dailyLove');
const dailyCareer = document.getElementById('dailyCareer');
const dailyWealth = document.getElementById('dailyWealth');
const dailyHealth = document.getElementById('dailyHealth');
const dailyAdvice = document.getElementById('dailyAdvice');

function initDailyCard() {
    // 显示今天的日期
    const today = new Date();
    const dateStr = today.toLocaleDateString('zh-CN', { 
        year: 'numeric', 
        month: 'long', 
        day: 'numeric',
        weekday: 'long'
    });
    if (dailyDate) {
        dailyDate.textContent = dateStr;
    }
    
    // 检查今天是否已经抽过牌
    const todayKey = getTodayKey();
    const dailyCards = JSON.parse(localStorage.getItem('dailyCards') || '{}');
    
    if (dailyCards[todayKey]) {
        // 今天已经抽过牌，显示结果
        displayDailyCard(dailyCards[todayKey]);
        if (dailyDrawBtn) {
            dailyDrawBtn.disabled = true;
            dailyDrawBtn.textContent = '今日已抽取';
        }
    } else {
        // 今天还没抽过牌，重置界面
        if (dailyResult) {
            dailyResult.classList.add('hidden');
        }
        if (dailyDrawBtn) {
            dailyDrawBtn.disabled = false;
            dailyDrawBtn.textContent = '抽取今日牌';
        }
    }
}

function getTodayKey() {
    const today = new Date();
    return `${today.getFullYear()}-${today.getMonth() + 1}-${today.getDate()}`;
}

if (dailyDrawBtn) {
    dailyDrawBtn.addEventListener('click', drawDailyCard);
}

if (dailyCardBack) {
    dailyCardBack.addEventListener('click', drawDailyCard);
}

function drawDailyCard() {
    const todayKey = getTodayKey();
    const dailyCards = JSON.parse(localStorage.getItem('dailyCards') || '{}');
    
    // 检查今天是否已经抽过
    if (dailyCards[todayKey]) {
        return;
    }
    
    // 播放翻牌音效
    playSound('flip');
    
    // 使用日期作为种子，确保同一天抽到相同的牌
    const seed = new Date().setHours(0, 0, 0, 0);
    const randomIndex = Math.floor((Math.sin(seed) * 10000) % tarotCards.length);
    const randomCard = tarotCards[Math.abs(randomIndex)];
    const isReversed = (seed % 2) === 0;
    
    // 保存今日牌
    dailyCards[todayKey] = {
        card: randomCard,
        isReversed: isReversed,
        date: new Date().toISOString()
    };
    
    localStorage.setItem('dailyCards', JSON.stringify(dailyCards));
    
    // 显示结果
    displayDailyCard(dailyCards[todayKey]);
    
    // 禁用按钮
    if (dailyDrawBtn) {
        dailyDrawBtn.disabled = true;
        dailyDrawBtn.textContent = '今日已抽取';
    }
    
    // 播放完成音效
    setTimeout(() => playSound('complete'), 500);
    
    // 保存到历史
    saveToHistory('daily', '每日一牌', [randomCard], [isReversed]);
}

function displayDailyCard(dailyData) {
    const { card, isReversed } = dailyData;
    const orientation = isReversed ? 'reversed' : 'upright';
    
    if (dailyCardSymbol) {
        dailyCardSymbol.textContent = card.symbol;
        if (isReversed) {
            dailyCardSymbol.style.transform = 'rotate(180deg)';
        } else {
            dailyCardSymbol.style.transform = 'rotate(0deg)';
        }
    }
    
    if (dailyCardName) {
        dailyCardName.textContent = card.name;
    }
    
    if (dailyCardOrientation) {
        dailyCardOrientation.textContent = isReversed ? '逆位 (Reversed)' : '正位 (Upright)';
        dailyCardOrientation.style.color = isReversed ? '#ff9999' : '#4caf50';
    }
    
    if (dailyCardDescription) {
        dailyCardDescription.textContent = card.description;
    }
    
    if (dailyLove) {
        dailyLove.textContent = card[orientation].love;
    }
    
    if (dailyCareer) {
        dailyCareer.textContent = card[orientation].career;
    }
    
    if (dailyWealth) {
        dailyWealth.textContent = card[orientation].wealth;
    }
    
    if (dailyHealth) {
        dailyHealth.textContent = card[orientation].health;
    }
    
    if (dailyAdvice) {
        const advices = [
            `今天的${card.name}提醒你：${card[orientation].future}`,
            `保持开放的心态，让${card.name}的能量引导你度过今天。`,
            `记住，塔罗牌只是一面镜子，真正的力量在你自己手中。`,
            `将今天的启示记在心中，它会在需要的时候给你指引。`
        ];
        dailyAdvice.textContent = advices[Math.floor(Math.random() * advices.length)];
    }
    
    if (dailyResult) {
        dailyResult.classList.remove('hidden');
    }
}

// ==================== 新功能：分享功能 ====================
const shareBtn = document.getElementById('shareBtn');
const shareDailyBtn = document.getElementById('shareDailyBtn');

if (shareBtn) {
    shareBtn.addEventListener('click', () => {
        shareReading('占卜结果');
    });
}

if (shareDailyBtn) {
    shareDailyBtn.addEventListener('click', () => {
        shareDailyCard();
    });
}

function shareReading(title) {
    const spreadName = getSpreadTypeName(currentSpread);
    let shareText = `🔮 ${spreadName}\n\n`;
    
    if (userQuestion) {
        shareText += `💭 问题：${userQuestion}\n\n`;
    }
    
    selectedCards.forEach((card, index) => {
        const orientation = cardOrientations[index] ? '逆位' : '正位';
        shareText += `${card.symbol} ${card.name} [${orientation}]\n`;
    });
    
    shareText += `\n✨ 来自塔罗牌占卜网站`;
    
    // 尝试使用Web Share API
    if (navigator.share) {
        navigator.share({
            title: title,
            text: shareText
        }).catch(err => {
            console.log('分享取消或失败', err);
            fallbackShare(shareText);
        });
    } else {
        fallbackShare(shareText);
    }
}

function shareDailyCard() {
    const todayKey = getTodayKey();
    const dailyCards = JSON.parse(localStorage.getItem('dailyCards') || '{}');
    const dailyData = dailyCards[todayKey];
    
    if (!dailyData) return;
    
    const { card, isReversed } = dailyData;
    const orientation = isReversed ? 'reversed' : 'upright';
    const orientationText = isReversed ? '逆位' : '正位';
    
    const shareText = `🌅 每日一牌 - ${new Date().toLocaleDateString('zh-CN')}\n\n` +
        `${card.symbol} ${card.name} [${orientationText}]\n\n` +
        `${card.description}\n\n` +
        `💕 爱情：${card[orientation].love}\n\n` +
        `💼 事业：${card[orientation].career}\n\n` +
        `✨ 来自塔罗牌占卜网站`;
    
    if (navigator.share) {
        navigator.share({
            title: '每日一牌',
            text: shareText
        }).catch(err => {
            console.log('分享取消或失败', err);
            fallbackShare(shareText);
        });
    } else {
        fallbackShare(shareText);
    }
}

function fallbackShare(text) {
    // 复制到剪贴板
    if (navigator.clipboard) {
        navigator.clipboard.writeText(text).then(() => {
            alert('✅ 占卜结果已复制到剪贴板！\n你可以粘贴到任何地方分享。');
        }).catch(err => {
            console.error('复制失败', err);
            showShareModal(text);
        });
    } else {
        showShareModal(text);
    }
}

function showShareModal(text) {
    // 创建一个临时文本框
    const textarea = document.createElement('textarea');
    textarea.value = text;
    textarea.style.position = 'fixed';
    textarea.style.opacity = '0';
    document.body.appendChild(textarea);
    textarea.select();
    
    try {
        document.execCommand('copy');
        alert('✅ 占卜结果已复制到剪贴板！');
    } catch (err) {
        alert('📋 请手动复制以下内容：\n\n' + text);
    }
    
    document.body.removeChild(textarea);
}
